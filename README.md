# MathieuBreier.github.io

## Run locally

Install dependencies (first time only):

```
npm install
bundle install
```

Start the dev server:

```
npm run dev
```

The site will open automatically in your browser via BrowserSync.

